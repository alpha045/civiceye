{
  "version": 2,
  "buildCommand": "if [ -d \"frontend\" ]; then (cd frontend && npm install && npm run build) && mkdir -p dist && cp -r frontend/dist/* dist/; else npm install && npm run build; fi",
  "outputDirectory": "dist",
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
